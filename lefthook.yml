pre-commit:
  parallel: true
  commands:
    beads:
      run: bd hooks run pre-commit
    luacheck:
      glob: "*.lua"
      run: luacheck plugin/ spec/
    stylua:
      glob: "*.lua"
      run: stylua --check plugin/ spec/
