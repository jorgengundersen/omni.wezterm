pre-commit:
  parallel: true
  commands:
    beads:
      run: bd hooks run pre-commit
    luacheck:
      glob: "*.lua"
      run: luacheck lua/ spec/
    stylua:
      glob: "*.lua"
      run: stylua --check lua/ spec/
